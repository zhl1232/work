---
name: project-seed
description: 批量生成项目内容并导入数据库
---

# 项目批量生成 Skill

本 Skill 用于帮助管理员批量生成项目内容，通过 AI 生成完整的项目数据并生成 SQL 迁移文件。

## 管理员信息

- **管理员 UUID**: `fc9f4384-2bb5-418e-a2e2-8c29bff6e7c5`
- **默认状态**: `approved`（管理员项目直接上线）

## 分类体系

### 主分类与子分类

| 主分类 | 子分类 |
|--------|--------|
| 科学 | 物理实验、化学实验、生物观察、天文地理 |
| 技术 | 编程入门、电子制作、机器人、3D打印 |
| 工程 | 机械结构、桥梁建造、简易机器、模型制作 |
| 艺术 | 绘画、手工、雕塑 |
| 数学 | 几何探索、逻辑游戏、数学魔术、统计实验 |
| 其他 | 生活技能、户外探索 |

## 难度星级

| 星级 | 描述 |
|------|------|
| ⭐ (1) | 入门级：材料常见、步骤极少、无需帮助 |
| ⭐⭐ (2) | 简单：步骤简单、轻微指导即可 |
| ⭐⭐⭐ (3) | 中等：需要一定技巧或专注力 |
| ⭐⭐⭐⭐ (4) | 进阶：步骤复杂或需要特殊技能 |
| ⭐⭐⭐⭐⭐ (5) | 挑战级：高难度技巧、多步骤协调 |
| 💫 (6) | 传说级：极高难度/需运气/长周期观察 |

## 项目数据结构

每个项目需包含：

```json
{
  "title": "项目标题",
  "description": "项目描述（100-300字）",
  "sub_category": "子分类名称",
  "difficulty_stars": 3,
  "duration": 60,
  "materials": ["材料1", "材料2", "材料3"],
  "tags": ["一年级", "人教版", "科学"],
  "steps": [
    {"title": "步骤标题", "description": "详细说明"},
    {"title": "步骤标题", "description": "详细说明"}
  ]
}
```

### 字段规范

- **title**: 简洁有吸引力，10-25字
- **description**: 描述项目内容和学习价值，100-300字
- **materials**: 3-8个常见易获取的材料
- **steps**: 4-10个步骤，每步描述清晰具体
- **duration**: 预计完成时间（分钟）
- **tags**: 项目标签，必填，例如 ["一年级", "人教版"]
- **cover_image**: 项目封面图（需调用 AI 生成）

## 图片生成说明

在生成项目时，请同时为每个项目生成一张封面图。

1. **构思提示词**: 根据项目标题和描述，构思一个生动、色彩明亮、适合儿童/学生的封面图提示词（Prompt）。风格建议为：3D 卡通风格，高饱和度，清晰的主体。
2. **调用工具**: 使用 `generate_image` 工具生成图片 (若支持)。
   - 如果用户提到使用 "Banana" 或其他 AI，请按用户要求处理。
   - 生成的图片应保存到 `public/projects/` 目录下（需确认目录存在）。
3. **获取路径**: 将生成的图片路径（或上传后的 URL）填入 SQL 的 `image_url` 字段。

## 生成 SQL 示例

当 AI 生成项目后，按以下格式输出 SQL：

```sql
-- ============================================
-- 批量导入项目：[主题描述]
-- 生成时间：[日期]
-- ============================================

-- 插入项目
INSERT INTO public.projects (title, description, author_id, sub_category_id, difficulty_stars, duration, status, image_url, tags)
VALUES 
  (
    '制作简易电磁铁',
    '学习电磁原理，用铁钉和铜线制作一个能吸起回形针的电磁铁，了解电流如何产生磁场。',
    'fc9f4384-2bb5-418e-a2e2-8c29bff6e7c5',
    (SELECT id FROM public.sub_categories WHERE name = '物理实验'),
    2,
    30,
    'approved',
    '/projects/electromagnet_cover.png',
    ARRAY['一年级', '人教版', '物理']
  )
RETURNING id;

-- 假设返回的项目 ID 为 @project_id

-- 插入材料
INSERT INTO public.project_materials (project_id, material, sort_order)
SELECT @project_id, material, sort_order FROM (VALUES
  ('铁钉（约10cm）', 1),
  ('漆包铜线（约2米）', 2),
  ('1.5V干电池', 3),
  ('回形针若干', 4)
) AS t(material, sort_order);

-- 插入步骤
INSERT INTO public.project_steps (project_id, title, description, sort_order)
SELECT @project_id, title, description, sort_order FROM (VALUES
  ('准备材料', '收集所需材料，确保铁钉表面干净', 1),
  ('缠绕铜线', '将铜线紧密缠绕在铁钉上，缠绕50-100圈', 2),
  ('连接电池', '将铜线两端分别连接电池正负极', 3),
  ('测试效果', '用电磁铁尝试吸起回形针', 4)
) AS t(title, description, sort_order);
```

## 使用方法

管理员请求示例：

> "按照 project-seed skill 生成 5 个科学-物理实验类项目，难度 2-3 星"

AI 会：
1. 根据规范生成项目 JSON
2. 转换为 SQL 迁移文件
3. 保存到 `supabase/migrations/` 目录
